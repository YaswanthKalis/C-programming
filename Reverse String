#include <stdio.h>
#include<string.h>

int main()
{
    char s[100];
    int i,count=0;
    
    fgets(s,100,stdin);
    int n = strlen(s)-1;
    
    for(i=n-1;i>=0;i--)
        printf("%c", s[i]);
    
    return 0;
}
